<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category_tag.name %>
</p>

<p>
  <strong>Slug:</strong>
  <%= @category_tag.slug %>
</p>

<%= link_to 'Edit', edit_category_tag_path(@category_tag) %> |
<%= link_to 'Back', category_tags_path %>
